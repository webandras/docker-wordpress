#!/bin/bash
source .env

# Install wordpress database
bin/cli bash -c "wp core install --url=${APP_DOMAIN} --title=${APP_NAME} --admin_user=${APP_DB_USER} --admin_password=${APP_DB_PASSWORD} --admin_email=${APP_ADMIN_EMAIL}"

